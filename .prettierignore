# 포매터 금지 구역 — 전부 '생성물'이다.
#
# 왜 필요한가: collector.py 는 data.json / data.js 를 매일 json.dumps(indent=2) 로 다시 쓰고,
# publish.sh 는 docs/ 를 dashboard/ 에서 통째로 복사한다. 포매터가 이 산출물을 손대면
# (예: docs/data.js 의 JSON 따옴표를 벗겨 JS 객체 리터럴로) 다음 수집이 원래대로 되돌리고,
# 그 왕복이 매일 수백 줄짜리 가짜 diff 로 발행 커밋에 쌓인다. 소스만 포맷한다.

# 발행본 전체 (publish.sh 가 만든다)
docs/

# 벤드링 라이브러리 — 남의 코드다. 포매터가 손댈 이유가 없고, 실제로 손댔다:
# uPlot.iife.min.js 가 49.9KB 1줄에서 105KB 3,465줄로 펼쳐져 커밋됐다.
# 동작은 같지만 방문자마다 2배 페이로드를 받고, 파일명이 .min.js 인데 미니파이가 아니게 된다.
dashboard/vendor/

# collector 산출물 (작업본)
data.json
research.json
dashboard/data.js
dashboard/research.js
dashboard/edge.js

# 시점불변 증거·파생물 — 손대지 않는다
backtest/ledger.jsonl
backtest/edge_status.json
backtest/history.json
