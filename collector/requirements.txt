# 자금 조류 수집기 의존성 — 없음.
#
# 네이버 금융 JSON API로 전환하면서 외부 패키지가 전부 빠졌다.
# collector.py는 표준 라이브러리(urllib, json, time)만 사용한다.
# (이전 설계의 pykrx는 KRX 포털 로그인 의무화로 폐기 — collector.py 상단 주석 참고)
#
# 실행: python collector/collector.py
